# 작업 내역 — 2026-09-17

핵심 루프 스펙 확정 → 작업 방식 재정렬(페이지 단위·Clean Architecture) → 첫 페이지(인트로+홈) 기획.

## 스펙 리뷰 확정 및 구현 계획(초안)
- 어제 대기 중이던 스펙 리뷰 4항목 확정: native_geofence / 가까운 매장 20개(앱 실행 시 재선별) /
  반경 150m / 테스트용 시드 소수. 설계 문서에 반영. (커밋 `593284f`)
- 서브에이전트 주도로 수평 레이어 13태스크 구현 계획 작성·시작 → **Task 1(의존성+시드 JSON)만 커밋**
  (`4570713`). riverpod은 Dart SDK ^3.7.2 제약으로 2.x(`^2.6.1`)로 확정.

## 작업 방식 재정렬 (사용자 지시)
- 페이지 단위 개발 + 페이지별 브레인스토밍(기록 필수) + Clean Architecture로 전환.
  수평 레이어 일괄 실행 중단.
- **CLAUDE.md 신설·확장** (커밋 `1b0d8f2`, `6a5452c`): 기획→디자인→개발 단계, 단계 전환 시 명시적 허가,
  계획에 단계별 예상 시간, 브레인스토밍 중 와이어프레임 HTML(`docs/design/`) 필수, 트러블슈팅 기록 규칙,
  FVM 고정 3.29.2(`fvm flutter`/`fvm dart`, PowerShell에서 실행).

## 첫 페이지 기획 (인트로 + 홈)
- 앱 이름 **다잊어 → 다이저** 변경(README 반영).
- 인트로: 매 실행 스플래시(로고+백그라운드 위치 고지, ~2초 후 홈 자동 전환, 권한 요청은 다음 페이지).
- 홈(쇼핑 목록): 상단 인라인 입력줄, 항목 탭 인라인 편집, 행별 수량 스테퍼(기본 1), 완료 시 취소선+하단
  이동, 스와이프 즉시 삭제, 하단 버전 표시.
- Clean Architecture 설계 확정(domain/data/presentation), 3가지 선택: package_info_plus / 유스케이스 생략 /
  DB 정렬.
- 산출물: 청사진 `docs/design/2026-09-17-intro-and-home.html`, 기획 기록 `docs/brainstorming/2026-09-17-intro-and-home.md`.

## 다음 단계
- [ ] writing-plans로 구현 계획 작성(단계별 예상 시간 포함)
- [ ] 사용자 허가 후 TDD 구현
