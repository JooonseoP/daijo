# 작업 내역 — 2026-09-16

daijo(다잊어) 프로젝트 초기 셋업 및 MVP 브레인스토밍.

## Git 저장소 연결
- 프로젝트 루트(`E:/Joonseo/dev_works/daijo`)를 git 저장소로 초기화하고 원격 `https://github.com/JooonseoP/daijo.git`에 연결.
- 원격의 초기 커밋(`bec13ec`, LICENSE 포함) 위에 로컬 Flutter 프로젝트를 올려 강제 푸시 없이 히스토리 연결.
- `main` 브랜치, 원격 추적 설정 완료. (커밋 `b348af0`)
- 130개 파일 커밋 — `.gitignore`로 `build/`, `.dart_tool/`, `.idea/`, `.fvm/versions` 등은 제외됨.

## 줄바꿈 규칙 고정
- 루트에 `.gitattributes` 추가 — 저장소는 LF 통일, Windows 스크립트(`.bat/.cmd/.ps1`)만 CRLF, 바이너리 예외 처리. (커밋 `7666367`)

## .gitignore 점검 및 보강
- 표준 Flutter 다층 `.gitignore` 구조 확인 (android/ios/macos/linux/windows 각자 보유). 시크릿/생성물 누출 없음 확인.
- 루트 `.gitignore`에 `.env`, `.env.*`, `coverage/` 예방 추가. (커밋 `7f0aa3b`)

## README 업데이트
- 기본 Flutter 템플릿 README를 프로젝트 실제 내용(위치 기반 지오펜싱 쇼핑 리마인더)으로 전면 교체.
- 소개/슬로건, 기획 배경(출입문 효과 등), 주요 기능, 기술 아키텍처, 지원 플랫폼, FVM 개발 환경 설정 포함. (커밋 `acfbe9c`)
- 근거: `lib/`의 기획·기술 명세서 docx.

## MVP 브레인스토밍 및 설계 문서
- superpowers **architectural 경로**로 진행. 전체 앱을 5개 서브프로젝트로 분해하고 **하나씩** 스펙→계획→구현 사이클로 짓기로 결정.
  1. 핵심 루프 (이번 대상) 2. 매장 인앱 모드 3. 스마트 입력 4. 아카이브&재등록 5. 백업&복원
- 서브프로젝트 1(핵심 루프) 브레인스토밍 확정: MVP=핵심 루프만 / Android 먼저 / 매장 좌표는 씨드 JSON 번들 내장 / 스택 = Riverpod + Drift(SQLite) + flutter_local_notifications + native_geofence(미확정).
- 설계 문서 작성·커밋 → `docs/superpowers/specs/2026-09-16-daijo-mvp-core-loop-design.md` (커밋 `da34200`).
- **상태**: 사용자 스펙 리뷰 대기. 코드는 아직 미작성.

## 다음 단계
- [ ] 스펙 문서 리뷰 — 미해결 확정: native_geofence 플러그인, 가까운 매장 N 기본값, 시드 매장 수/출처, 지오펜스 반경 기본값
- [ ] 승인 후 writing-plans 스킬로 구현 계획 작성
- [ ] TDD로 핵심 루프 구현
